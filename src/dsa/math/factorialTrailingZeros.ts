/*

Given an integer n, return the number of trailing zeroes in n!.

Note that n! = n * (n - 1) * (n - 2) * ... * 3 * 2 * 1.

Input: n = 5
Output: 1
Explanation: 5! = 120, one trailing zero.

*/

export const trailingZeroes = (n: number) => {
  console.log(n);
};

1;

console.log(trailingZeroes(15));
