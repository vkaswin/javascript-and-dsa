import { ITreeNode, buildBinaryTree } from "../tree";

const diameterOfBinaryTree = (root: ITreeNode | null) => {};

let tree = buildBinaryTree([2, 1, 3, 4, 5]);
console.log(diameterOfBinaryTree(tree));
